name: flutter_aplicacion1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  # Bluetooth
  flutter_blue_plus: ^1.34.0

  # Permisos
  permission_handler: ^11.3.1

  cupertino_icons: ^1.0.8

  # Base de datos Isar (FIX ESTABILIZADO)
  isar: 3.1.0+1
  isar_flutter_libs: 3.1.0+1
  path_provider: ^2.1.2

  # HTTP
  http: ^1.6.0

  # Fechas / formato
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  isar_generator: 3.1.0+1
  build_runner: ^2.4.0

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/